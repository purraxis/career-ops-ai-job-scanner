# Application Material Templates

These templates define stable structures for future document generation without storing private career data in the public repository.

Use private career sources at runtime:

- `private/cv.md`
- `private/config/profile.yml`
- `private/config/resume_rules.yml`
- private writing samples, if available

Generated documents should stay outside git in ignored folders such as `output/`.
